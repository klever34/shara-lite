import React from 'react';
import {View} from 'react-native';

export const BNPLClientTransactionListItem = ({
  item,
  onPress,
}: {
  item: any;
  onPress(): void;
}) => {
  return <View></View>;
};
